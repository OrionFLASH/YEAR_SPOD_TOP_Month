# Описание процесса нормализации показателей и выбора лучшего месяца

**Дата создания:** 2025-12-11  
**Версия:** 1.0

---

## 📋 Содержание

1. [Процесс нормализации показателей](#процесс-нормализации-показателей)
2. [Расчет Score и мест](#расчет-score-и-мест)
3. [Выбор лучшего месяца](#выбор-лучшего-месяца)
4. [Пример расчета](#пример-расчета)

---

## 🔄 Процесс нормализации показателей

### Общая схема

Нормализация выполняется **отдельно для каждого показателя** (OD, RA, PS) и **для каждого КМ** (табельного номера) **горизонтально по месяцам**.

### Шаг 1: Подготовка данных

Для каждого показателя (OD, RA, PS) собираются все значения по месяцам для конкретного КМ:

```
Для КМ #12345:
OD: M-1 = 100, M-2 = 150, M-3 = 120, M-4 = 180
RA: M-1 = 50,  M-2 = 60,  M-3 = 55,  M-4 = 70
PS: M-1 = 200, M-2 = 250, M-3 = 220, M-4 = 300
```

### Шаг 2: Определение направления показателя

Каждый показатель имеет направление:
- **MAX** - большее значение лучше (например, доходы)
- **MIN** - меньшее значение лучше (например, расходы)

Направление задается в конфигурации через параметр `indicator_direction`:
- OD: `indicator_direction = "MAX"` (по умолчанию)
- RA: `indicator_direction = "MAX"` (по умолчанию)
- PS: `indicator_direction = "MAX"` (по умолчанию)

### Шаг 3: Нахождение минимума и максимума

Для каждого КМ и каждого показателя находятся:
- **min** - минимальное значение среди всех месяцев (игнорируя NaN)
- **max** - максимальное значение среди всех месяцев (игнорируя NaN)
- **range** = max - min (диапазон значений)

**Пример:**
```
Для КМ #12345, показатель OD:
min = 100 (M-1)
max = 180 (M-4)
range = 180 - 100 = 80
```

### Шаг 4: Нормализация значений

#### Формула для MAX (больше = лучше):

```
normalized = (value - min) / range
```

**Результат:** значение от 0 до 1, где:
- 0 = минимальное значение (худший месяц)
- 1 = максимальное значение (лучший месяц)

#### Формула для MIN (меньше = лучше):

```
normalized = (max - value) / range
```

**Результат:** значение от 0 до 1, где:
- 0 = максимальное значение (худший месяц)
- 1 = минимальное значение (лучший месяц)

**Пример для MAX:**
```
OD: M-1 = (100 - 100) / 80 = 0.0
    M-2 = (150 - 100) / 80 = 0.625
    M-3 = (120 - 100) / 80 = 0.25
    M-4 = (180 - 100) / 80 = 1.0
```

### Шаг 5: Обработка особых случаев

#### Случай 1: Только один месяц с данными (не нулями)

Если у КМ только один месяц с ненулевыми данными:
- Этот месяц получает нормализованное значение **1.0**
- Остальные месяцы (нули) получают **0.0**

**Пример:**
```
OD: M-1 = 0, M-2 = 150, M-3 = 0, M-4 = 0
→ M-2 = 1.0, остальные = 0.0
```

#### Случай 2: Все значения одинаковы

Если все значения одинаковы (включая все нули):
- Все месяцы получают нормализованное значение **0.5**

**Пример:**
```
OD: M-1 = 100, M-2 = 100, M-3 = 100, M-4 = 100
→ Все = 0.5
```

#### Случай 3: Деление на ноль (range = 0)

Если range < 1e-10 (все значения одинаковы или очень близки):
- Защита: range заменяется на 1.0
- Все месяцы получают **0.5**

### Шаг 6: Ограничение диапазона

После нормализации все значения ограничиваются диапазоном [0, 1] с помощью `clip(0.0, 1.0)` для защиты от погрешностей вычислений.

---

## 📊 Расчет Score и мест

### Шаг 1: Расчет Score для каждого месяца

После нормализации для каждого месяца рассчитывается **Score** - взвешенная сумма нормализованных значений:

```
Score (M-N) = OD_norm (M-N) × weight_od + RA_norm (M-N) × weight_ra + PS_norm (M-N) × weight_ps
```

**Веса по умолчанию:**
- `weight_od = 0.33` (33%)
- `weight_ra = 0.33` (33%)
- `weight_ps = 0.34` (34%)

**Пример:**
```
Для КМ #12345, месяц M-2:
OD_norm (M-2) = 0.625
RA_norm (M-2) = 0.5
PS_norm (M-2) = 0.6

Score (M-2) = 0.625 × 0.33 + 0.5 × 0.33 + 0.6 × 0.34
           = 0.20625 + 0.165 + 0.204
           = 0.57525
```

### Шаг 2: Расчет ранга (места) для каждого месяца

Для каждого КМ рассчитывается **ранг** (место) каждого месяца на основе Score.

**Метод расчета:**
- Используется `rank()` с параметрами:
  - `method='min'` - при одинаковых Score присваивается минимальный ранг
  - `ascending=False` - большее значение Score = лучшее место (ранг 1)
  - `na_option='keep'` - NaN остаются NaN

**Результат:** ранг от 1 (лучший) до N (худший), где N - количество месяцев.

**Пример:**
```
Для КМ #12345:
Score (M-1) = 0.2  → Место = 4
Score (M-2) = 0.575 → Место = 2
Score (M-3) = 0.35  → Место = 3
Score (M-4) = 0.8   → Место = 1

Лучший месяц: M-4 (ранг 1)
```

### Шаг 3: Определение лучшего месяца

**Лучший месяц** - это месяц(ы) с рангом **1** (наивысший Score).

Если несколько месяцев имеют ранг 1 (одинаковый Score), применяется дополнительная логика:

#### Логика выбора при нескольких лучших месяцах:

1. **Если месяцы подряд идут** (например, 2, 3, 4) и **значения OD, RA, PS одинаковые**:
   - Выбирается только **первый месяц** из последовательности (2)

2. **Если есть несколько групп подряд идущих месяцев**:
   - Группа 1: месяцы 2, 3, 4 с одинаковыми значениями → выбирается 2
   - Группа 2: месяцы 7, 8, 9 с одинаковыми значениями (но отличающимися от группы 1) → выбирается 7
   - Результат: **2, 7**

3. **Если все месяцы разные**:
   - Выбираются **все месяцы** с рангом 1

**Сравнение значений:**
- Значения считаются одинаковыми, если разница < 1e-10 (для float)
- NaN считаются равными друг другу

---

## 📝 Пример расчета

### Исходные данные для КМ #12345

**Расчетные значения (лист "Расчет"):**

| Месяц | OD      | RA      | PS      |
|-------|---------|---------|---------|
| M-1   | 100.0   | 50.0    | 200.0   |
| M-2   | 150.0   | 60.0    | 250.0   |
| M-3   | 120.0   | 55.0    | 220.0   |
| M-4   | 180.0   | 70.0    | 300.0   |

**Направления показателей:**
- OD: MAX (больше = лучше)
- RA: MAX (больше = лучше)
- PS: MAX (больше = лучше)

**Веса:**
- weight_od = 0.33
- weight_ra = 0.33
- weight_ps = 0.34

---

### Шаг 1: Нормализация показателя OD

**Находим min и max:**
```
min = 100.0 (M-1)
max = 180.0 (M-4)
range = 180.0 - 100.0 = 80.0
```

**Нормализация (MAX):**
```
OD_norm (M-1) = (100.0 - 100.0) / 80.0 = 0.0
OD_norm (M-2) = (150.0 - 100.0) / 80.0 = 0.625
OD_norm (M-3) = (120.0 - 100.0) / 80.0 = 0.25
OD_norm (M-4) = (180.0 - 100.0) / 80.0 = 1.0
```

**Результат нормализации OD:**

| Месяц | OD      | OD_norm |
|-------|---------|---------|
| M-1   | 100.0   | 0.0     |
| M-2   | 150.0   | 0.625   |
| M-3   | 120.0   | 0.25    |
| M-4   | 180.0   | 1.0     |

---

### Шаг 2: Нормализация показателя RA

**Находим min и max:**
```
min = 50.0 (M-1)
max = 70.0 (M-4)
range = 70.0 - 50.0 = 20.0
```

**Нормализация (MAX):**
```
RA_norm (M-1) = (50.0 - 50.0) / 20.0 = 0.0
RA_norm (M-2) = (60.0 - 50.0) / 20.0 = 0.5
RA_norm (M-3) = (55.0 - 50.0) / 20.0 = 0.25
RA_norm (M-4) = (70.0 - 50.0) / 20.0 = 1.0
```

**Результат нормализации RA:**

| Месяц | RA      | RA_norm |
|-------|---------|---------|
| M-1   | 50.0    | 0.0     |
| M-2   | 60.0    | 0.5     |
| M-3   | 55.0    | 0.25    |
| M-4   | 70.0    | 1.0     |

---

### Шаг 3: Нормализация показателя PS

**Находим min и max:**
```
min = 200.0 (M-1)
max = 300.0 (M-4)
range = 300.0 - 200.0 = 100.0
```

**Нормализация (MAX):**
```
PS_norm (M-1) = (200.0 - 200.0) / 100.0 = 0.0
PS_norm (M-2) = (250.0 - 200.0) / 100.0 = 0.5
PS_norm (M-3) = (220.0 - 200.0) / 100.0 = 0.2
PS_norm (M-4) = (300.0 - 200.0) / 100.0 = 1.0
```

**Результат нормализации PS:**

| Месяц | PS      | PS_norm |
|-------|---------|---------|
| M-1   | 200.0   | 0.0     |
| M-2   | 250.0   | 0.5     |
| M-3   | 220.0   | 0.2     |
| M-4   | 300.0   | 1.0     |

---

### Шаг 4: Расчет Score для каждого месяца

**Формула:** `Score = OD_norm × 0.33 + RA_norm × 0.33 + PS_norm × 0.34`

```
Score (M-1) = 0.0 × 0.33 + 0.0 × 0.33 + 0.0 × 0.34
            = 0.0 + 0.0 + 0.0
            = 0.0

Score (M-2) = 0.625 × 0.33 + 0.5 × 0.33 + 0.5 × 0.34
            = 0.20625 + 0.165 + 0.17
            = 0.54125

Score (M-3) = 0.25 × 0.33 + 0.25 × 0.33 + 0.2 × 0.34
            = 0.0825 + 0.0825 + 0.068
            = 0.233

Score (M-4) = 1.0 × 0.33 + 1.0 × 0.33 + 1.0 × 0.34
            = 0.33 + 0.33 + 0.34
            = 1.0
```

**Результат расчета Score:**

| Месяц | OD_norm | RA_norm | PS_norm | Score    |
|-------|---------|---------|---------|----------|
| M-1   | 0.0     | 0.0     | 0.0     | 0.0      |
| M-2   | 0.625   | 0.5     | 0.5     | 0.54125  |
| M-3   | 0.25    | 0.25    | 0.2     | 0.233    |
| M-4   | 1.0     | 1.0     | 1.0     | 1.0      |

---

### Шаг 5: Расчет ранга (места) для каждого месяца

**Сортируем по Score (убывание):**
1. M-4: Score = 1.0 → Место = 1
2. M-2: Score = 0.54125 → Место = 2
3. M-3: Score = 0.233 → Место = 3
4. M-1: Score = 0.0 → Место = 4

**Результат расчета мест:**

| Месяц | Score    | Место |
|-------|----------|-------|
| M-1   | 0.0      | 4     |
| M-2   | 0.54125  | 2     |
| M-3   | 0.233    | 3     |
| M-4   | 1.0      | 1     |

---

### Шаг 6: Определение лучшего месяца

**Лучший месяц:** M-4 (единственный месяц с рангом 1)

**Результат:** В колонке "Лучший месяц" будет указано: **4**

---

## 📋 Итоговая таблица для КМ #12345

### Лист "Нормализация"

| Месяц | OD_norm | RA_norm | PS_norm |
|-------|---------|---------|---------|
| M-1   | 0.0     | 0.0     | 0.0     |
| M-2   | 0.625   | 0.5     | 0.5     |
| M-3   | 0.25    | 0.25    | 0.2     |
| M-4   | 1.0     | 1.0     | 1.0     |

### Лист "Места и выбор"

| Месяц | Score (M-N) | Место (M-N) | Лучший месяц |
|-------|-------------|-------------|--------------|
| M-1   | 0.0         | 4           |              |
| M-2   | 0.54125     | 2           |              |
| M-3   | 0.233       | 3           |              |
| M-4   | 1.0         | 1           | 4            |

---

## 🔍 Пример с несколькими лучшими месяцами

### Исходные данные для КМ #67890

**Расчетные значения:**

| Месяц | OD      | RA      | PS      |
|-------|---------|---------|---------|
| M-2   | 100.0   | 50.0    | 200.0   |
| M-3   | 100.0   | 50.0    | 200.0   |
| M-4   | 100.0   | 50.0    | 200.0   |
| M-7   | 150.0   | 60.0    | 250.0   |
| M-8   | 150.0   | 60.0    | 250.0   |
| M-9   | 150.0   | 60.0    | 250.0   |

**Нормализация:**
- Все месяцы M-2, M-3, M-4 имеют одинаковые значения → получают одинаковые нормализованные значения
- Все месяцы M-7, M-8, M-9 имеют одинаковые значения (но другие) → получают одинаковые нормализованные значения

**Score:**
- M-2, M-3, M-4: одинаковый Score (например, 0.5)
- M-7, M-8, M-9: одинаковый Score (например, 0.8) - выше, чем у M-2, M-3, M-4

**Ранг:**
- M-2, M-3, M-4: ранг 2
- M-7, M-8, M-9: ранг 1 (лучшие)

**Выбор лучшего месяца:**
- Группа M-7, M-8, M-9: подряд идут, значения одинаковые → выбирается только **M-7**

**Результат:** В колонке "Лучший месяц" будет указано: **7**

---

## 📌 Важные замечания

1. **Нормализация выполняется горизонтально** - для каждого КМ отдельно по его месяцам
2. **Веса показателей** можно настроить в конфигурации (по умолчанию: 0.33, 0.33, 0.34)
3. **Направление показателей** можно настроить для каждого показателя отдельно (MAX или MIN)
4. **При одинаковых Score** используется метод `rank(method='min')` - присваивается минимальный ранг
5. **При нескольких лучших месяцах** применяется логика группировки подряд идущих месяцев с одинаковыми значениями

---

## 🔧 Формулы в кратком виде

### Нормализация (MAX):
```
normalized = (value - min) / (max - min)
```

### Нормализация (MIN):
```
normalized = (max - value) / (max - min)
```

### Score:
```
Score = OD_norm × weight_od + RA_norm × weight_ra + PS_norm × weight_ps
```

### Ранг:
```
Ранг = rank(Score, method='min', ascending=False)
```

### Лучший месяц:
```
Лучший месяц = месяц(ы) с рангом 1 (после обработки подряд идущих месяцев)
```

---

**Конец документа**
