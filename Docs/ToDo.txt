✅ Проверить идет ли параллельная загрузка данных? есть предположение что тот же OD грузится не параллельно а последовательно, но возможно это из-за числа строк в нем. Проверить - ВЫПОЛНЕНО: добавлено логирование для диагностики параллельной загрузки
✅ Когда в лог при деталоьном логировании ТН пишется ФИО его тоже надо маскировать: от каждого слова в ФИО оставляем первые 2 буквы далее три звездочки и последняя 1 буква: Петров Иван Владимироваич -> Пе***в Ив***н Вл***ч - ВЫПОЛНЕНО: добавлена функция _mask_fio
✅ Надо для детального логирования ТН предусмотреть возможность указать несколько ТН для детального логирования в параметре дял этого надо использовать массив и если в массиве указано несколько ТН то для нескольких ТН логирование и проводить детальное, а если массив пустой это означает что детальное логирование не требуется - ВЫПОЛНЕНО: DEBUG_TAB_NUMBER изменен на Optional[List[str]]
✅ Во всех местах когда в лог пишем ФИО его надо маскировать: от каждого слова в ФИО оставляем первые 2 буквы далее три звездочки и последняя 1 буква: Петров Иван Владимироваич -> Пе***в Ив***н Вл***ч (например еще в после merge для файла... Пример ) - ВЫПОЛНЕНО: ФИО маскируется через _mask_sensitive_data во всех логах
✅ Надо в ИТОГ добавить колонку с подсчетом числа уникальных ИНН (код клиента) у данного ТН в исходных файлах (можно брать с листа RAW данные) - ВЫПОЛНЕНО: добавлена колонка "Количество уникальных ИНН" в final_df
✅ Когда в лог пишется для табельного найдено столько-то вариантов в этой строке сумма не должна маскироваться, и сумму надо писать как число с разделителем разрядов и два знака после запятой) - ВЫПОЛНЕНО: сумма форматируется с разделителем разрядов (1 234 567,89)
